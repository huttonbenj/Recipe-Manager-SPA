# Recipe Manager Monorepo Environment Variables

# Database Configuration (shared across packages)
DATABASE_URL=postgresql://postgres:your_password@localhost:5432/recipe_manager
DATABASE_TEST_URL=postgresql://postgres:your_password@localhost:5432/recipe_manager_test

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-here-at-least-32-characters-long
JWT_REFRESH_SECRET=your-super-secret-refresh-jwt-key-here-at-least-32-characters-long

# Server Configuration
PORT=3001
NODE_ENV=development

# Client Configuration
VITE_API_URL=http://localhost:3001

# Docker Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password
POSTGRES_DB=recipe_manager
POSTGRES_TEST_DB=recipe_manager_test

# Development Tools
LOG_LEVEL=info
VITE_ENABLE_DEBUG=true

# Instructions:
# 1. Copy this file to .env in the root directory
# 2. Update the values with your actual configuration
# 3. Never commit the .env file to version control
# 4. Each package may have its own .env.example file with package-specific variables
