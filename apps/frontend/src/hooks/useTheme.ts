/**
 * Custom hook for theme management
 * Re-exports the useTheme hook from ThemeContext for convenient importing
 */
export { useTheme, type ColorTheme, type DisplayMode, type ThemeConfig } from '../context/ThemeContext'; 