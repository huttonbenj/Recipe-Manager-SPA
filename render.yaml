services:
  - name: recipe-manager-backend
    type: web
    runtime: node
    region: oregon
    plan: free
    branch: main
    rootDir: ./
    buildCommand: "npm install && npm run build --workspace=apps/backend"
    startCommand: "npm run start --workspace=apps/backend"
    healthCheckPath: /api/health
    preDeployCommand: "npm run db:deploy --workspace=apps/backend"
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        fromDatabase:
          name: recipe-manager-db
          property: connectionString
      - key: JWT_SECRET
        generateValue: true
      - key: JWT_EXPIRES_IN
        value: 7d
      - key: FRONTEND_URL
        fromService:
          name: recipe-manager-frontend
          type: web
          property: url
  - name: recipe-manager-frontend
    type: static
    region: oregon
    plan: free
    branch: main
    rootDir: ./
    buildCommand: "npm install && npm run build --workspace=apps/frontend"
    staticPublishPath: apps/frontend/dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
    envVars:
      - key: VITE_API_BASE_URL
        fromService:
          name: recipe-manager-backend
          type: web
          property: url

databases:
  - name: recipe-manager-db
    plan: free
    region: oregon
    postgresMajorVersion: "14" 